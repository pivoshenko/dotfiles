Host *
  # Use macOS keychain to store passphrases
  UseKeychain yes
  # Automatically add keys to the SSH agent
  AddKeysToAgent yes
  # Specify the default identity file for authentication
  IdentityFile ~/.ssh/id_ed25519
