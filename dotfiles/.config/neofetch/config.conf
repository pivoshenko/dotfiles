print_info () {
    info "${cl0} 󰊠" host
    info "${cl0} " distro
    info "${cl0} 󰘚" cpu
    info "${cl0} 󰡷" gpu
    info "${cl0} 󰍛" memory
    info "${cl0} 󰋊" disk

    prin "\n"

    info "${cl0} " song

    info
}

separator=" "

# set formt of the RAM usage
memory_percent="on"
memory_unit="Gib"

# set format of the disk usage
disk_percent="on"
disk_show=("/")

# set custom distro logo
image_backend="ascii"
image_source="${HOME}/.config/neofetch/assets/catppuccin"

# define colors
catppuccin_macchiato_mauve="\033[38;5;183m"
cl0="${catppuccin_macchiato_mauve}"

# set custom colors
colors=(distro)
ascii_colors=(distro)
