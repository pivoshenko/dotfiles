# =================================================
# Default commands
# =================================================

alias cat="bat --paging never --decorations never --plain"
alias ls="eza -a --level=1 --icons --color=always"

# =================================================
# External tools
# =================================================

alias ld="lazydocker"
alias lg="lazygit"
alias zj="zellij"
